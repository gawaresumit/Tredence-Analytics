# Tredence-Analytics
Design a neural network that prunes itself during training to reduce size and computation. Instead of post-training pruning, it dynamically removes weak connections using regularization and a custom training loop, adapting its structure on the fly for efficiency without sacrificing performance.
